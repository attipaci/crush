1. Parallel to use ExecutorService.
	Parallel.process(pool)


2. Parallelize pipeline tasks


3. Parallelize source model
	- n persistent source copies. Parallel in scan or integration?


4. Parallelize initialization tasks (where possible).
